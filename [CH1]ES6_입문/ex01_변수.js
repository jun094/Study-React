// 일반 변수
let value=1;
console.log(value);

//상수 --> 바뀔 수 없음
const a = 1;
a =2; //오류 !

//문자열
let text = '가나다라';
console.log(text);

// null vs undefined
let friend = null; // 아예 없다.
let friend = undefined // 아직 없다. 아직 지정해주지 않았다.
